OpsLab {{ .Chart.AppVersion }} was installed as release {{ .Release.Name }}.

Check the release:
  helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  kubectl get pods -n {{ .Release.Namespace }}

Open the API documentation:
  kubectl port-forward -n {{ .Release.Namespace }} service/{{ include "opslab.fullname" . }}-api 18001:{{ .Values.api.service.port }}

Then visit http://localhost:18001/docs

